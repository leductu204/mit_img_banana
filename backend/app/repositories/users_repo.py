# repositories/users_repo.py
"""Placeholder repository for user data (e.g., Supabase/Postgres)."""

def get_user(user_id: str):
    # Dummy user data
    return {"user_id": user_id, "username": "example_user", "credits": 0}

def create_user(user_id: str, username: str):
    # Placeholder create
    return {"user_id": user_id, "username": username, "credits": 0}
